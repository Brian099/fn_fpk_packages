#!/bin/bash

echo "第三方应用中心更新初始化..."

# Stop the service before upgrade
if [ -x "${TRIM_APPDEST:-/var/apps/appcenterThirdParty/target}/main" ]; then
    echo "Stopping service before upgrade..."
    "${TRIM_APPDEST:-/var/apps/appcenterThirdParty/target}/main" stop
else
    # Try alternative path
    if [ -x "${TRIM_APPDEST:-/var/apps/appcenterThirdParty/target}/cmd/main" ]; then
        echo "Stopping service before upgrade..."
        "${TRIM_APPDEST:-/var/apps/appcenterThirdParty/target}/cmd/main" stop
    else
        echo "Service control script not found, skipping stop"
    fi
fi

exit 0
