#!/bin/bash

# T3MT视频下载 - 配置回调脚本
# 在配置完成后执行

echo "T3MT配置回调执行中..." >> "${TRIM_TEMP_LOGFILE}"

# 处理授权目录变更
if [ -n "${TRIM_DATA_ACCESSIBLE_PATHS}" ]; then
    echo "授权目录已更新: ${TRIM_DATA_ACCESSIBLE_PATHS}" >> "${TRIM_TEMP_LOGFILE}"
fi

echo "T3MT配置完成" >> "${TRIM_TEMP_LOGFILE}"
exit 0
