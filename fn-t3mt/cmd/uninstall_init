#!/bin/bash

# T3MT视频下载 - 卸载初始化脚本
# 在卸载向导之前执行

echo "T3MT卸载初始化..." >> "${TRIM_TEMP_LOGFILE}"

# 停止运行中的容器
if docker ps | grep -q "t3mt"; then
    echo "停止T3MT容器..." >> "${TRIM_TEMP_LOGFILE}"
    docker stop t3mt >> "${TRIM_TEMP_LOGFILE}" 2>&1
fi

exit 0
