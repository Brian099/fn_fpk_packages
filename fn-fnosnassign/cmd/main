#!/bin/bash

# ==============================================================================
# ImageAdmin Service Controller (Hardened Stop Logic)
# ==============================================================================

export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:$PATH"

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
APP_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
TRIM_APPDEST="${TRIM_APPDEST:-${APP_ROOT}/app}"
TRIM_PKGVAR="${TRIM_PKGVAR:-${APP_ROOT}/var}"

CONF_FILE="${TRIM_PKGVAR}/config.yaml"
PID_FILE="${TRIM_PKGVAR}/service.pid"
LOG_FILE="${TRIM_PKGVAR}/info.log"
SOCK_FILE="${TRIM_PKGVAR}/fnosnassign.sock"

get_conf() {
    local key="$1"
    local default="$2"
    if [ -f "${CONF_FILE}" ]; then
        local val
        val=$(grep "^$key:" "${CONF_FILE}" | head -n1 | cut -d':' -f2- | sed 's/[",[:space:]]//g')
        echo "${val:-$default}"
    else
        echo "$default"
    fi
}

is_running() {
    if [ -f "$PID_FILE" ]; then
        local pid=$(cat "$PID_FILE")
        if kill -0 "$pid" 2>/dev/null; then
            return 0
        fi
    fi
    return 1
}

stop() {
    echo "正在停止服务..."
    
    # 1. 优先使用 PID 文件进行优雅停止
    if [ -f "$PID_FILE" ]; then
        local pid=$(cat "$PID_FILE")
        if kill -0 "$pid" 2>/dev/null; then
            kill "$pid" 2>/dev/null
            local count=0
            while kill -0 "$pid" 2>/dev/null && [ $count -lt 5 ]; do
                sleep 1
                count=$((count + 1))
            done
        fi
    fi
    
    # 2. 如果仍在运行，执行清理
    echo "正在执行残留清理..."
    pkill -9 -f "fnosnassign_x86" >/dev/null 2>&1
    pkill -9 -f "fnosnassign_aar" >/dev/null 2>&1
    
    rm -f "$PID_FILE"
    rm -f "$SOCK_FILE"
    sleep 1
    echo "停止完成。"
}

start() {
    # 稳健的架构判定逻辑
    local arch=$(uname -m)
    local arch_dir=""
    local bin_suffix=""

    case "$arch" in
        x86_64)
            arch_dir="amd64"
            bin_suffix="x86_64"
            ;;
        aarch64|arm64)
            arch_dir="arm64"
            bin_suffix="aarch64"
            ;;
        *)
            echo "❌ 错误: 不支持的系统架构 ($arch)。本应用仅支持 x86_64 和 arm64。"
            exit 1
            ;;
    esac

    local binary="${TRIM_APPDEST}/server/${arch_dir}/fnosnassign_${bin_suffix}"
    
    if [ ! -f "$binary" ]; then
        echo "❌ 错误: 未找到对应的二进制文件: $binary"
        return 1
    fi

    chmod +x "$binary"
    mkdir -p "${TRIM_PKGVAR}"
    chmod 755 "${TRIM_PKGVAR}" 2>/dev/null
    
    rm -f "$SOCK_FILE"
    cd "${TRIM_PKGVAR}"
    
    echo "正在启动服务 (${arch})..."
    # 使用绝对路径启动，防止进程名查找失败
    nohup "$binary" -config "$CONF_FILE" -sock "$SOCK_FILE" >> "$LOG_FILE" 2>&1 &
    
    echo $! > "$PID_FILE"
    sleep 2
    [ -f "$LOG_FILE" ] && chmod 666 "$LOG_FILE"
    [ -S "$SOCK_FILE" ] && chmod 777 "$SOCK_FILE"
    
    if is_running; then
        echo "服务成功启动。"
    else
        echo "❌ 启动失败，请查看日志: $LOG_FILE"
        return 1
    fi
}

case "$1" in
    start)   start ;;
    stop)    stop ;;
    restart) stop; sleep 1; start ;;
    status)  if is_running; then echo "Running"; exit 0; else echo "Stopped"; exit 3; fi ;;
    *)       echo "Usage: $0 {start|stop|restart|status}"; exit 1 ;;
esac
